<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage"
        android:maxSdkVersion="32" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BudgetPlan"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ui.budget.CreateBudgetActivity"
            android:parentActivityName=".presentation.ui.MainActivity"
            android:theme="@style/Theme.BudgetPlan.M3"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.transaction.CreateTransactionActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.MainActivity"
            android:theme="@style/Theme.BudgetPlan.M3" />
        <activity
            android:name=".presentation.ui.category.CreateCategoryActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.MainActivity"
            android:theme="@style/Theme.BudgetPlan.M3" />
        <activity
            android:name=".presentation.ui.account.CreateNewAccountActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.MainActivity"
            android:theme="@style/Theme.BudgetPlan.M3" />
        <activity
            android:name=".presentation.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BudgetPlan.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>